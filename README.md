# Running Tests for the Capicua Project

Clone the repository:

```bash
git clone git@github.com:joaquim-manzini/learn-capicua.git
```

Install it with yarn:

```bash
yarn install
```

Run tests:

```bash
yarn test
```
